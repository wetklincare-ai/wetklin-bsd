export default function WhyChoose() {
  const reasons = [
    "Gentle on all fabrics",
    "Premium cleaning standards",
    "Free pickup & delivery",
    "Safe for delicate fabrics",
  ];

  return (
    <section className="py-16 px-6 md:px-16 bg-wetklinBlue/10 text-center">
      <h2 className="text-2xl md:text-4xl font-bold mb-8 text-gray-800">Why Choose Wetklin</h2>
      <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-6">
        {reasons.map((text, index) => (
          <div key={index} className="bg-white rounded-2xl p-6 shadow hover:shadow-lg transition">
            <p className="font-medium text-gray-700">{text}</p>
          </div>
        ))}
      </div>
    </section>
  );
}
